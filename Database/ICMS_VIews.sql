SET ANSI_NULLS ON
GO

SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW PS_SIS_IC_LI_VW
AS
SELECT
	SIS_LI_ID,
	SIS_LO_ID,
	SIS_LI_TEXT,
	SEQNUM,
	EFF_STATUS,
	CREATEOPRID,
	CREATED_DTTM,
	LAST_UPDT_OPRID,
	LAST_UPDT_DTTM
FROM PS_SIS_IC_LI

GO


SET ANSI_NULLS ON
GO

SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW PS_SIS_IC_LO_VW
AS
SELECT
	SIS_LO_ID,
	SIS_LOS_ID,
	EFF_STATUS,
	CREATEOPRID,
	CREATED_DTTM,
	LAST_UPDT_OPRID,
	LAST_UPDT_DTTM,
	SIS_LO_TEXT,
	SIS_LO_INSTR
FROM PS_SIS_IC_LO

GO


SET ANSI_NULLS ON
GO

SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW PS_SIS_IC_LOQ_VW
AS
SELECT
	SIS_LOQ_ID,
	SIS_LO_ID,
	SIS_R_STATUS,
	SIS_INPUT_TYPE,
	SEQNUM,
	EFF_STATUS,
	CREATEOPRID,
	CREATED_DTTM,
	LAST_UPDT_OPRID,
	LAST_UPDT_DTTM,
	SIS_LOQ_TEXT
FROM PS_SIS_IC_LOQ

GO


SET ANSI_NULLS ON
GO

SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW PS_SIS_IC_LOS_VW
AS
SELECT
	SIS_LOS_ID,
	SIS_INTERN_TYPE,
	DESCR50,
	EFF_STATUS,
	CREATEOPRID,
	CREATED_DTTM,
	LAST_UPDT_OPRID,
	LAST_UPDT_DTTM
FROM PS_SIS_IC_LOS

GO


SET ANSI_NULLS ON
GO

SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW PS_SIS_IC_RQ_VW
AS
SELECT
	SIS_RQ_ID,
	SIS_RS_ID,
	SEQNUM,
	EFF_STATUS,
	CREATEOPRID,
	CREATED_DTTM,
	LAST_UPDT_OPRID,
	LAST_UPDT_DTTM,
	SIS_RQ_TEXT,
	SIS_RQ_GUIDE
FROM PS_SIS_IC_RQ

GO


SET ANSI_NULLS ON
GO

SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW PS_SIS_IC_RS_VW
AS
SELECT
	SIS_RS_ID,
	SIS_INTERN_TYPE,
	DESCR50,
	CREATEOPRID,
	CREATED_DTTM,
	LAST_UPDT_OPRID,
	LAST_UPDT_DTTM,
	SIS_POST_HEADER
FROM PS_SIS_IC_RS

GO


SET ANSI_NULLS ON
GO

SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW PS_SIS_IC_STDRA_VW
AS
SELECT
	EMPLID,
	SIS_INTPART_ID,
	SIS_RQ_ID,
	CREATEOPRID,
	CREATED_DTTM,
	LAST_UPDT_OPRID,
	LAST_UPDT_DTTM,
	SIS_RQ_ANSWER
FROM PS_SIS_IC_STDRA

GO


SET ANSI_NULLS ON
GO

SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW PS_SIS_IC_STDRH_VW
AS
SELECT
	EMPLID,
	SIS_INTPART_ID,
	SIS_R_STATUS,
	CREATEOPRID,
	CREATED_DTTM,
	LAST_UPDT_OPRID,
	LAST_UPDT_DTTM
FROM PS_SIS_IC_STDRH

GO


SET ANSI_NULLS ON
GO

SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW PS_SIS_IC_STDRLO_VW
AS
SELECT
	EMPLID,
	SIS_INTPART_ID,
	SIS_LO_ID,
	SIS_IS_LO_ON_TGT,
	SIS_IS_LO_ACHV,
	CREATEOPRID,
	CREATED_DTTM,
	LAST_UPDT_OPRID,
	LAST_UPDT_DTTM
FROM PS_SIS_IC_STDRLO

GO


SET ANSI_NULLS ON
GO

SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW PS_SIS_IC_STDRLOA_VW
AS
SELECT
	EMPLID,
	SIS_INTPART_ID,
	SIS_LO_ID,
	SIS_LOQ_ID,
	CREATEOPRID,
	CREATED_DTTM,
	LAST_UPDT_OPRID,
	LAST_UPDT_DTTM,
	SIS_LOQ_ANSWER
FROM PS_SIS_IC_STDRLOA

GO


